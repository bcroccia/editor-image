# Config

## Environment Variables

- `ANTHROPIC_API_KEY` **required** — font-pipeline/auto_font.py
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` (has default) — image-service/web/.env.local
- `NEXT_PUBLIC_SUPABASE_URL` (has default) — image-service/web/.env.local

## Config Files

- `image-service/web/next.config.ts`
